6th pg

pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Check out code from Git repository using credentials
                git(
                    url: 'https://github.com/RUSHYOP/dev.git',
                    branch: 'main'
                )
            }
        }
        stage('Build') {
            steps {
                // Use bat for Windows systems to run Maven build
                bat 'mvn clean package'
            }
        }
        stage('Test') {
            steps {
                // Use bat for Windows systems to run Maven tests
                bat 'mvn test'
            }
        }
    }

    post {
        always {
            // Archive test reports (ensure the reports are generated by Maven Surefire Plugin)
            junit '**/target/surefire-reports/*.xml'
        }
        success {
            echo 'Build and tests succeeded!'
        }
        failure {
            echo 'Build or tests failed.'
        }
    }
}


6th pg repo - https://github.com/RUSHYOP/program-6

